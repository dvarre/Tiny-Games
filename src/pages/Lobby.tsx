const Lobby = ({ roomId } : { roomId: string }) => {
    return (
        <div>
            <h1>Lobby Page</h1>
            <span>Room id : { roomId }</span>
        </div>
    );

};

export default Lobby; 